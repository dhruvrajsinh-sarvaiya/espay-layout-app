import { StyleSheet } from 'react-native';
import R from '../../../R';

export default StyleSheet.create({
  container: {
    paddingVertical: R.dimens.dashboardPaddingTop,
    borderRadius: 0,
    justifyContent: 'center',
  },
});
